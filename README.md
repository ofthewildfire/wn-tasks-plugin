# Tasks

A simple task manager for Winter CMS. Admins assign tasks to front-end users
from the backend; each user sees their own tasks on a kanban-style board
(To Do / In Progress / Done) on the frontend, and can leave comments on
each task.

